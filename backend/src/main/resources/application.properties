server.port=7070

# Jasypt - password set via env var JASYPT_ENCRYPTOR_PASSWORD or JVM arg -Djasypt.encryptor.password
jasypt.encryptor.password=commerce

# CORS Configuration
allowed.origins=${ALLOWED_ORIGINS:http://localhost:14200,http://localhost:4200,http://127.0.0.1:4200,https://shopping-frontend.greengrass-56c2de65.eastus.azurecontainerapps.io}

# Database Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://shopping-mysql:3306/shopping_app?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:shopuser}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ENC(IggorKaN8GomCNvVARJ7QwgS7Cb8Bx+UNxDvvbq1B9WrbGEF5d9zKOMbdPyxE/kX)}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=ENC(aOq0HmAVe7ePNJbSFKMXWicxr9CSywUS8Nwmv63FrXGe3884qzc6EKBS6Cg9BCa3Aq5Y7LAMHHUHDdMVOyq990faQdKOnQlTSdvTLdUsf/ypGDhOAC4KQihq5rhDhkkH)
jwt.expiration=86400000

# Jasypt - password set via env var JASYPT_ENCRYPTOR_PASSWORD or JVM arg -Djasypt.encryptor.password
jasypt.encryptor.password=commerce

# OpenAI Configuration
# Set your API key here (encrypted with Jasypt) or via env var OPENAI_API_KEY
# Google Gemini Configuration
google.gemini.api.key=${GEMINI_API_KEY:dummy_key}
google.gemini.chat.model=${GOOGLE_GEMINI_CHAT_MODEL:gemini-2.0-flash}
google.gemini.chat.api.url=${GOOGLE_GEMINI_CHAT_API_URL:https://generativelanguage.googleapis.com/v1beta/models}
google.gemini.embedding.model=${GOOGLE_GEMINI_EMBEDDING_MODEL:gemini-embedding-001}
google.gemini.embedding.api.url=${GOOGLE_GEMINI_EMBEDDING_API_URL:https://generativelanguage.googleapis.com/v1beta/models}


# Redis Configuration
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:shopping-redis}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}

# Redis Vector Store
spring.ai.vectorstore.redis.uri=${SPRING_DATA_REDIS_URL:redis://shopping-redis:6379}
spring.ai.vectorstore.redis.index=products
spring.ai.vectorstore.redis.prefix=product:

# Kafka Configuration (Disabled for now)
spring.kafka.bootstrap-servers=${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
spring.kafka.consumer.group-id=${SPRING_KAFKA_CONSUMER_GROUP_ID:shopping-group}
spring.kafka.listener.auto-startup=false
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# Mail Configuration
spring.mail.host=${SPRING_MAIL_HOST:localhost}
spring.mail.port=${SPRING_MAIL_PORT:1025}
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# Multipart Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

